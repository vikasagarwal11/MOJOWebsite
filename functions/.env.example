# Copy to `.env` in this folder (gitignored). Firebase CLI loads `functions/.env` when you deploy;
# the repo root `.env` is for Vite/web only and is NOT available to Cloud Functions at runtime.
GEMINI_API_KEY=
OPENAI_API_KEY=
